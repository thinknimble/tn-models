---
"@thinknimble/tn-models": patch
---

Point `repository` and `homepage` at the tn-models repository. Both still named the old tn-models-fp repository, so the links on the npm page sent readers to the previous name.
